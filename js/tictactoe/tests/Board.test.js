//const Board = require('../src').Board;

test('draws an x when the player clicks a square', () => {
    expect(1).toBe(1);
});

test('draws an o when the cpu clicks a square', () => {
    expect(1).toBe(1);
});